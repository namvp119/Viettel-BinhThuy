<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Viettel Cần Thơ - Quản Trị Hệ Thống" Height="750" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#F4F6F9" FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ViettelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF0033"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#CC0029"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" BorderBrush="Transparent" BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FFE5EA"/>
                    <Setter Property="Foreground" Value="#FF0033"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF0033" Padding="20,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="White" Width="40" Height="40" CornerRadius="8" Margin="0,0,15,0">
                    <TextBlock Text="V" Foreground="#FF0033" FontWeight="Black" FontSize="26" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="VIETTEL CẦN THƠ - CRM SYSTEM" Foreground="White" FontSize="22" FontWeight="Black" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1" Margin="10" Background="Transparent" BorderThickness="0">

            <TabItem Header="👥 QUẢN LÝ KHÁCH HÀNG" FontSize="16" FontWeight="Bold">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,15">
                        <Button x:Name="btnRefresh" Content="🔄 Làm mới" Style="{StaticResource ViettelButtonStyle}" Click="btnRefresh_Click" Width="130" Margin="0,0,10,0"/>
                        <Button x:Name="btnExportExcel" Content="📊 Xuất Excel" Style="{StaticResource ViettelButtonStyle}" Background="#00CC44" Click="btnExportExcel_Click" Width="130"/>
                    </StackPanel>

                    <Border Grid.Row="1" Background="White" CornerRadius="10" BorderBrush="#E5E7EB" BorderThickness="1">
                        <DataGrid x:Name="dgvKhachHang" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F0F0" RowBackground="#FFFFFF" AlternatingRowBackground="#FAFAFA" SelectionMode="Single">
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="⏳ Chờ xử lý" Tag="0" Click="MenuTrangThai_Click"/>
                                    <MenuItem Header="📞 Đang khảo sát" Tag="1" Click="MenuTrangThai_Click"/>
                                    <MenuItem Header="✅ Hoàn thành" Tag="2" Click="MenuTrangThai_Click"/>
                                    <Separator/>
                                    <MenuItem Header="❌ Hủy đơn này" Tag="3" Foreground="Red" FontWeight="Bold" Click="MenuTrangThai_Click"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã" Binding="{Binding Id}" Width="50"/>
                                <DataGridTextColumn Header="Họ và Tên" Binding="{Binding HoTen}" Width="150"/>
                                <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SoDienThoai}" Width="120" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Nhu Cầu" Binding="{Binding NhuCau}" Width="*"/>
                                <DataGridTextColumn Header="Địa Chỉ" Binding="{Binding DiaChi}" Width="*"/>
                                <DataGridTextColumn Header="Thời Gian ĐK" Binding="{Binding ThoiGianDangKy, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="140"/>
                                <DataGridTextColumn Header="Trạng Thái" Binding="{Binding TenTrangThai}" Width="130" FontWeight="Bold"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="📦 QUẢN LÝ GÓI CƯỚC" FontSize="16" FontWeight="Bold">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="350"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,15,0" BorderBrush="#E5E7EB" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="THÔNG TIN GÓI CƯỚC" FontWeight="Black" FontSize="18" Foreground="#333" Margin="0,0,0,20"/>

                            <TextBlock Text="Mã Gói (Tự tăng):" Foreground="Gray" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtIdGoi" IsEnabled="False" Height="35" Padding="5" Margin="0,0,0,15" Background="#F0F0F0"/>

                            <TextBlock Text="Tên Gói Cước (*):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtTenGoi" Height="35" Padding="5" Margin="0,0,0,15"/>

                            <TextBlock Text="Loại Dịch Vụ:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cbxLoaiDichVu" Height="35" Padding="5" Margin="0,0,0,15" FontSize="14" VerticalContentAlignment="Center">
                                <ComboBoxItem Content="Wifi" IsSelected="True"/>
                                <ComboBoxItem Content="TV"/>
                                <ComboBoxItem Content="Camera"/>
                                <ComboBoxItem Content="DoanhNghiep"/>
                            </ComboBox>

                            <TextBlock Text="Giá Tiền (VNĐ) (*):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtGiaTien" Height="35" Padding="5" Margin="0,0,0,15"/>

                            <TextBlock Text="Mô Tả:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtMoTa" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="5" Margin="0,0,0,25"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="btnThemGC" Grid.Column="0" Content="Thêm" Style="{StaticResource ViettelButtonStyle}" Margin="0,0,5,0" Click="btnThemGC_Click"/>
                                <Button x:Name="btnSuaGC" Grid.Column="1" Content="Sửa" Style="{StaticResource ViettelButtonStyle}" Background="#FF9900" Margin="5,0,5,0" Click="btnSuaGC_Click"/>
                                <Button x:Name="btnXoaGC" Grid.Column="2" Content="Xóa" Style="{StaticResource ViettelButtonStyle}" Background="#333333" Margin="5,0,0,0" Click="btnXoaGC_Click"/>
                            </Grid>
                            <Button x:Name="btnLamMoiForm" Content="Làm mới Form" Background="Transparent" Foreground="#FF0033" BorderThickness="1" BorderBrush="#FF0033" Height="35" Margin="0,15,0,0" Cursor="Hand" Click="btnLamMoiForm_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="White" CornerRadius="10" BorderBrush="#E5E7EB" BorderThickness="1">
                        <DataGrid x:Name="dgvGoiCuoc" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F0F0" RowBackground="#FFFFFF" AlternatingRowBackground="#FAFAFA" SelectionMode="Single" SelectionChanged="dgvGoiCuoc_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã" Binding="{Binding Id}" Width="50"/>
                                <DataGridTextColumn Header="Tên Gói" Binding="{Binding TenGoi}" Width="150" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Dịch Vụ" Binding="{Binding LoaiDichVu}" Width="100"/>
                                <DataGridTextColumn Header="Giá Tiền" Binding="{Binding GiaTien, StringFormat=\{0:N0\}}" Width="120" Foreground="#FF0033" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Mô Tả" Binding="{Binding MoTa}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="📊 THỐNG KÊ TỔNG QUAN" FontSize="16" FontWeight="Bold">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="TỔNG QUAN TÌNH HÌNH KINH DOANH" FontWeight="Black" FontSize="20" Foreground="#333333" Margin="0,0,0,20"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6*"/>
                            <ColumnDefinition Width="4*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="20" Margin="0,0,20,0" BorderBrush="#E5E7EB" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Tỷ lệ Trạng thái Đơn hàng" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" Foreground="Gray"/>
                                <lvc:PieChart x:Name="pieChartTrangThai" LegendLocation="Bottom" Height="400" Hoverable="True" DataTooltip="{x:Null}"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Border Background="#FFE5EA" CornerRadius="15" Padding="25" Margin="0,0,0,20" BorderBrush="#FF0033" BorderThickness="2">
                                <StackPanel>
                                    <TextBlock Text="TỔNG SỐ KHÁCH HÀNG" Foreground="#FF0033" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtTongKhach" Text="0" Foreground="#FF0033" FontWeight="Black" FontSize="45"/>
                                </StackPanel>
                            </Border>

                            <Border Background="#E5F9ED" CornerRadius="15" Padding="25" Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="ĐÃ HOÀN THÀNH LẮP ĐẶT" Foreground="#00CC44" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtDaHoanThanh" Text="0" Foreground="#00CC44" FontWeight="Black" FontSize="45"/>
                                </StackPanel>
                            </Border>

                            <Border Background="#FFF4E5" CornerRadius="15" Padding="25">
                                <StackPanel>
                                    <TextBlock Text="ĐANG CHỜ / KHẢO SÁT" Foreground="#FF9900" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtDangCho" Text="0" Foreground="#FF9900" FontWeight="Black" FontSize="45"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>